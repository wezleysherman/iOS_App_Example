enum inputType {LINK, SEARCH_ITEM}

class SearchInput {
    var sInput = "empty link"
    
    init (sInput : String) {
        self.sInput = sInput
    }
    
    // Checks to see if the input is a link or search term
    func checkInput() {
        
    }
}