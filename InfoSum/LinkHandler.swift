

var linkHandler : HTMLHandler = HTMLHandler(link: "www.google.com")

class HTMLHandler : Link{
    var htmlString = "empty html"
    var pageBody = "empty body"
    var pageTitle = "empty title"
    
  
}
